version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:14.17 # Specify the Node.js version you need
    steps:
      - checkout
      - run: npm install
      - run: npm test

workflows:
  version: 2
  build-and-test:
    jobs:
      - build
